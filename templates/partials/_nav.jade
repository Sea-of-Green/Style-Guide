header.toggle
  i.fa.fa-bars.toggle__button
  h3.toggle__title Sea of Green Style Guide
  hr

nav.nav(role="navigation")

  .nav__header
    h3.nav__title Sea of Green Style Guide
    hr

  ul.nav__links.nav__links--parent

    li.nav__link.nav__link--section
      a(href="/") Introduction

    each page, title in collections.pages

      if page.title != "Introduction"
        li.nav__link.nav__link--section
          a(href="/#{page.slug}")= page.title

        if page.children
          ul.nav__links.nav__links--child
            each collection, name in collections
              if name == page.children
                for post in collection
                  li.nav__link.nav__link--part
                    a(href="/#{name}/#{post.slug}")= post.title
